spring.datasource.url=jdbc:h2:mem:database
spring.datasource.driver-class-name=org.h2.Driver
spring.sql.init.data-locations=

# Toss Payments 테스트 설정 (실제 호출 없이 컨텍스트 로딩용)
toss.base-url=https://api.tosspayments.com
toss.client-key=test_gck_docs_Ovk5rk1EwkEbP0W43n07xlzm
toss.secret-key=test_gsk_docs_OaPz8L5KdmQXkzRz3y47BMw6
toss.connect-timeout-ms=${TOSS_CONNECT_TIMEOUT_MS:500}
toss.read-timeout-ms=${TOSS_READ_TIMEOUT_MS:500}

toss.max-attempts=3

# Rate Limit 은 기본적으로 넉넉히 풀어 두고(다른 테스트 방해 금지),
# 거부 시나리오를 보는 테스트만 @TestPropertySource 로 한도를 낮춰 검증한다.
rate-limit.capacity=100000
rate-limit.refill-per-second=100000
