spring:
  profiles:
    active: test

---

server:
  port: 63155

spring:
  datasource:
    url: jdbc:h2:mem:test;DATABASE_TO_LOWER=TRUE
    username: sa


  jpa:
    hibernate:
      ddl-auto: create-drop
      naming:
        physical-strategy: org.hibernate.boot.model.naming.PhysicalNamingStrategyStandardImpl

    show-sql: true
    properties:
      hibernate:
        format_sql: true
    defer-datasource-initialization: true

  h2:
    console:
      enabled: true
      path: /h2-console
  config:
    activate:
      on-profile: test

jwt:
  secret-key: gotjrgoeh2rjtdmsqufakfdldkslqslekgoehrgkwlaktpdy=
  expiration: 1800000
