spring:
  profiles:
    default: jpa
  h2:
    console:
      enabled: true
  datasource:
    url: jdbc:h2:mem:database;MODE=MySQL;DB_CLOSE_DELAY=-1
  jpa:
    show-sql: true
    properties:
      hibernate:
        format_sql: true
